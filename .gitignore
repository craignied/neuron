.DS_Store
.env
credentials.json
client_secrets.json
*.p8
# Machine/session-local Claude Code permissions -- never shipped.
# (gitignore has no trailing comments: a pattern is the whole line.)
.claude/
# Craig's own working notes -- untracked on purpose; do not sweep them into a commit
NOTES.md
gui_notes.md
handoff.md
large_cohort_evaluation_notes.md
WALKTHROUGH_REFRESH.md
.vscode/
__pycache__/
*.pyc
venv/
build/
tests/oracle/bin/
tests/oracle/runs/
tests/golden/runs/
tests/tools/runs/
# Engine log files, written next to whatever dataset a session loads
neuron.log
neuron_actions.log
model.txt
tests/gui/runs/
# LaTeX build products from `latexmk -pdf manifest.tex` in docs/tex. The
# published manual is docs/manifest.pdf, copied there deliberately; everything
# below is regenerated by the build and must never be swept into a commit.
docs/tex/manifest.aux
docs/tex/manifest.fdb_latexmk
docs/tex/manifest.fls
docs/tex/manifest.idx
docs/tex/manifest.ilg
docs/tex/manifest.ind
docs/tex/manifest.log
docs/tex/manifest.out
docs/tex/manifest.pdf
docs/tex/manifest.toc
# Tier-3 cross-validation artifacts, written next to the loaded dataset
cv_predictions.csv
cv_metrics.csv
cv_locked_predictions.csv
cv_run.json
